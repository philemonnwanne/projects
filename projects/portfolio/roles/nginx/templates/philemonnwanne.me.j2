server {
       listen {{ http_port }};
       listen [::]:{{ http_port }};

       server_name {{ http_host }};

       root {{ document_root }};
       index index.html;

       location / {
               try_files $uri $uri/ =404;
       }
}
